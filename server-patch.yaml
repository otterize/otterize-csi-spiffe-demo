spec:
  template:
    metadata:
      labels:
        credentials-operator.otterize.com/create-aws-role:  "true"
    spec:
      containers:
      - name: server
        env:
        - name: AWS_REGION
          value: "us-west-2"
